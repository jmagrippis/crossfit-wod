{
  "name": "crossfit-wod-web",
  "version": "0.1.0",
  "private": true,
  "description": "Web version to see the WODs",
  "scripts": {
    "start": "parcel src/index.html -p 3010",
    "build": "parcel build src/index.html",
    "deploy": "yarn build && gcloud app deploy --project=crossfit-wod-0"
  },
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.10.3",
    "typescript": "^3.2.2"
  },
  "dependencies": {
    "reset-css": "^4.0.1"
  }
}
